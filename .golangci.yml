# Aligned with ossf/scorecard's .golangci.yml (the upstream graft target), with
# four repo-specific adaptations:
#   - gci import prefix points at this module;
#   - scorecard-only exclusion paths (probes/…) and the fileparser wrapcheck glob
#     are dropped (they do not exist here);
#   - goheader is not enabled (its year-matching template is brittle across years;
#     headers are kept consistent by convention instead);
#   - the gosec excludes list drops G703-G706, which upstream carries but which
#     aren't real gosec rule IDs; golangci-lint v2.8.0's `config verify` rejects
#     them outright, failing the lint job before any linter runs.
version: "2"
run:
  concurrency: 6
  timeout: 10m
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  new-from-rev: ""
linters:
  enable:
    - asasalint
    - asciicheck
    - bidichk
    - bodyclose
    - contextcheck
    - copyloopvar
    - dogsled
    - durationcheck
    - err113
    - errchkjson
    - errname
    - errorlint
    - exhaustive
    - forbidigo
    - gocheckcompilerdirectives
    - gochecknoinits
    - gochecksumtype
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - godot
    - godox
    - gomoddirectives
    - gomodguard
    - goprintffuncname
    - gosec
    - gosmopolitan
    - lll
    - loggercheck
    - makezero
    - mirror
    - misspell
    - musttag
    - nakedret
    - nestif
    - nilerr
    - nilnesserr
    - noctx
    - nolintlint
    - paralleltest
    - predeclared
    - protogetter
    - reassign
    - recvcheck
    - rowserrcheck
    - spancheck
    - sqlclosecheck
    - staticcheck
    - testableexamples
    - testifylint
    - thelper
    - tparallel
    - unconvert
    - usestdlibvars
    - usetesting
    - whitespace
    - wrapcheck
    - zerologlint
  disable:
    - exhaustruct
    - testpackage
  settings:
    errcheck:
      check-type-assertions: true
      check-blank: true
    exhaustive:
      default-signifies-exhaustive: true
    forbidigo:
      forbid:
        - pattern: "^fmt\\.Print.*$"
          msg: "Do not commit print statements. Output to stdout interferes with users who redirect JSON results to files."
    gocritic:
      disable-all: true
      enabled-checks:
        - appendAssign
        - argOrder
        - badCall
        - badCond
        - caseOrder
        - codegenComment
        - commentedOutCode
        - deprecatedComment
        - dupArg
        - dupBranchBody
        - dupCase
        - dupSubExpr
        - exitAfterDefer
        - flagDeref
        - flagName
        - mapKey
        - nilValReturn
        - octalLiteral
        - offBy1
        - sloppyLen
        - sloppyTypeAssert
        - weakCond
        - appendCombine
        - hugeParam
        - rangeExprCopy
        - rangeValCopy
        - assignOp
        - boolExprSimplify
        - captLocal
        - commentedOutImport
        - commentFormatting
        - defaultCaseOrder
        - docStub
        - elseif
        - emptyFallthrough
        - hexLiteral
        - ifElseChain
        - methodExprCall
        - newDeref
        - regexpMust
        - singleCaseSwitch
        - switchTrue
        - typeAssertChain
        - typeSwitchVar
        - underef
        - unlabelStmt
        - unlambda
        - unslice
        - valSwap
        - wrapperFunc
        - builtinShadow
        - importShadow
        - initClause
        - nestingReduce
        - paramTypeCombine
        - ptrToRefParam
        - typeUnparen
        - unnecessaryBlock
    godox:
      keywords:
        - BUG
        - FIXME
        - HACK
    gosec:
      excludes:
        - G115
    govet:
      enable:
        - fieldalignment
    nolintlint:
      require-specific: true
    staticcheck:
      checks:
        - "all"
        - "-QF1003"
    wrapcheck:
      # Errors crossing this module's own package boundaries are already
      # contextualized at their source (e.g. "store: ...", "tokens: ..."), so
      # re-wrapping them would only double the prefix. wrapcheck still enforces
      # wrapping for third-party (Scorecard, gocloud, stdlib) errors.
      ignore-package-globs:
        - github.com/ossf/scorecard-infra/*
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - err113
          - funlen
          - goconst
          - gocyclo
          - lll
          - wrapcheck
        path: (.+)_test\.go
      # The imported results API (design W7). ossf/scorecard-webapp ran
      # `default: none` with an explicit enable list; this config descends from
      # ossf/scorecard's and enables strictly more. Every linter below fired
      # only because of that difference -- none of it is a defect the imported
      # code introduced, and all of it is in hand-written source rather than the
      # generated tree, so a generated-path exclusion would not reach it.
      #
      # The tree is behavior-frozen until cutover completes, so the choice was
      # to rewrite frozen code to satisfy a config it never ran under, or to
      # apply its own historical rules to it. This is the latter, and it is
      # deliberately scoped to api/ so nothing else inherits the laxer set.
      #
      # REMOVE THIS BLOCK when the freeze lifts, and fix what it surfaces --
      # tracked as task 5.7 in openspec/changes/migrate-api.
      - linters:
          - err113
          - errcheck
          - errorlint
          - gocognit
          - gocritic
          - nolintlint
          - testifylint
          - wrapcheck
        path: ^api/
      # govet is deliberately NOT in the list above. Upstream's `default: none`
      # left it off (its govet settings block is vestigial), but leaving it on
      # here caught a real defect the split would otherwise have carried in: a
      # non-constant format string in api/main.go, which upstream CI never saw
      # because it only ran `go test` inside app/server. Only fieldalignment is
      # excluded -- reordering struct fields in a frozen tree is churn, and it
      # is a packing nit rather than a behavior bug.
      - linters:
          - govet
        text: "fieldalignment:"
        path: ^api/
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/ossf/scorecard-infra)
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
